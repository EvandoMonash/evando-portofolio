// Example middleware stub (kept minimal). Useful for security headers or redirects later.
import type { NextRequest } from 'next/server';

export function middleware(_req: NextRequest) {
    // No‑op for now
}

